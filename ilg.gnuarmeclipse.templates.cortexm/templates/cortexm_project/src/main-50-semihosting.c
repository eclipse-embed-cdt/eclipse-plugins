
      ++seconds;

      // Count seconds on the trace device.
      trace_printf ("Second %d\n", seconds);
    }
  return 0;
}

#pragma GCC diagnostic pop

// ----------------------------------------------------------------------------
